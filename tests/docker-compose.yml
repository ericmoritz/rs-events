version: '3'

services:
  db:
    image: postgres
  web:
    build: 
      context: ..
    command: bash ./run-server.sh
    environment:
      DATABASE_URL: postgres://postgres@db/
    ports: 
      - "8080:8080"
    depends_on: 
      - db
  bdd:
    build: 
      context: .
    command: node_modules/.bin/cucumber-js
    depends_on:
      - web
